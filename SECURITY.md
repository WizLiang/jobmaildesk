# 安全说明

当前 Windows 定制版仍处于候选验证阶段。历史版本验收记录不代表本次构建已经完成相同验证。

## 数据与凭据

- IMAP 仅以只读方式连接，使用 BODY.PEEK 读取邮件。
- 邮件授权码保存在系统凭据库，不写入配置或源码仓库。
- 邮件正文只在内存中解析，保留必要的结构化信息及脱敏证据。
- 本机数据位置由用户选择；清除范围见 [隐私清除说明](docs/PRIVACY_RESET.md)。
- 仓库不应包含真实邮箱、授权码、私钥、个人数据库、私人链接或原始邮件。

## 升级与报告

Windows 更新仅从 WizLiang/jobmaildesk 的 GitHub Release 下载，通过 HTTPS 和 SHA-256 检查文件完整性；校验值与包来自同一发布，不等同于独立代码签名。自动检查默认关闭，安装需用户明确确认。

更新器拒绝路径穿越、目录链接和不完整程序包，不覆盖数据目录。替换失败尝试恢复旧程序，并保留备份供回退。手动升级应退出旧程序并更换完整程序文件夹。

报告问题时请使用虚构或脱敏样例，不在公开 Issue、PR 或日志附件中粘贴授权码、原始邮件或完整个人数据目录。涉及凭据泄露的问题应先在对应服务撤销凭据，再通过维护者提供的私密渠道沟通。

维护者提交前应执行测试、scripts/secret-scan.ps1 和 git diff --check，并人工复核待提交文件。自动扫描不能代替人工审查。
