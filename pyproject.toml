[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "job-mail-desk"
version = "0.7.0rc1"
description = "Privacy-first, Markdown-native job email desk."
readme = "README.md"
requires-python = ">=3.12,<3.13"
license = { text = "MIT" }
dependencies = [
  "apscheduler>=3.11,<4",
  "cryptography>=50,<51",
  "keyring>=25.7,<26",
  "pillow>=11,<13",
  "pystray>=0.19,<1",
  "pywebview>=5,<7",
  "pyyaml>=6,<7",
  "tzdata>=2026.1",
]

[project.scripts]
jobmaildesk = "job_mail_desk.bootstrap:main"

[dependency-groups]
dev = [
  "py2app>=0.28,<1; sys_platform == 'darwin'",
  "pyinstaller>=6.18,<7; sys_platform == 'win32'",
  "pytest>=9,<10",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
job_mail_desk = [
  "ui/*.html",
  "ui/*.css",
  "ui/*.js",
  "identity_data/*.yml",
]
