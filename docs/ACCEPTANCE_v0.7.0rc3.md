# Windows 维护版候选验收

日期：2026-09-20。范围：Windows x64，未签名 prerelease。

- 本机隔离目录完整回归：604 passed、1 skipped（用户快照项），耗时 162.64 秒；既有 Pillow 弃用提示不影响结果。
- 版本工具及 Windows 资源专项：39 项通过；覆盖版本同步、重复字段/回退拒绝、磁盘写失败回滚和回滚失败保留原始备份。
- 新增更新维护回归覆盖失败检查重试、重启恢复提示、通道/不降级、缓存地址重校验、ZIP 清理边界和保留就绪包。
- 浏览器模拟设置桥接确认“当前版本：0.7.0rc3”可见；检查不连接邮箱或执行安装。界面改动只有设置中的版本文字。
- 版本同步与一致性检查、秘密扫描、JavaScript 语法检查和 PowerShell 打包脚本语法检查通过。
- 使用旧 rc1 的实际 EXE 核对打包拒绝路径：不能把它标成 rc3；没有生成误标的安装包。
- 版本标签前由功能分支 Windows CI 验证构建、两个 EXE ProductVersion、打包和隔离空数据 smoke；标签工作流再次执行同样门禁后发布两个资产。

发布流程记录：[Actions](https://github.com/WizLiang/jobmaildesk/actions)，对应分支 `agent/release-maintenance`、标签 `v0.7.0rc3`。发布后需下载最终 ZIP 复验 SHA-256、包内版本、更新 helper 与空数据自检，检查 rc2 能识别 rc3。

本轮未连接真实邮箱、读取授权码或替换正在使用的程序。没有完成真实在用 GUI 的退出重启、长期使用、签名和安全软件兼容性验收；保留上一版相同 RC 限制，不宣称稳定版。功能分支尚需通过 PR 合并 `main`。
