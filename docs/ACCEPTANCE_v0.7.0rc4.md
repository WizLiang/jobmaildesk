# Windows 扫描可靠性候选验收

日期：2026-09-20。范围：Windows x64，未签名 prerelease。

- 本机 Windows 隔离目录完整回归：704 passed、1 skipped，耗时 259.54 秒；既有 Pillow 弃用提示不影响结果。
- 版本一致性、秘密扫描、JavaScript 语法和 Git 差异检查通过，版本为 0.7.0rc4。
- 本轮只使用虚构邮箱与独立测试目录；没有连接真实邮箱、读取授权码或替换正在使用的程序。
- 浏览器加载实际 HTML/CSS/JavaScript 与合成桥接数据，检查默认 480×740 窗口、108% 与 125% 字号、自动过滤列表、恢复反馈、空列表、当前扫描账号和读取/识别失败提示；预览结束后关闭临时页面与服务。
- 保存后重启提示的合成界面检查通过：提示保持可见、显示新生效账号、授权码输入框清空。
- 回归范围包括邮箱切换与保存失败、账号/文件夹独立覆盖、停用补扫、首次及历史补扫失败重试、窄补扫不掩盖缺口、人工终态保护、过滤摘要脱敏、恢复后确认申请与待办。
- 受控线程回归验证排队的旧扫描在切换账号后失效，不会把旧服务器设置与新凭据组合；提交后导出或清理失败不会误报设置未保存，调度器应用失败则暂停运行并提示重启。
- 分支 Windows CI 执行完整测试、构建、EXE 版本校验、打包与隔离空数据自检；这些远端项目在本记录提交时待执行，通过后才创建唯一版本标签。标签工作流再次执行同样门禁再发布两个资产。

构建记录：[Actions](https://github.com/WizLiang/jobmaildesk/actions)，对应分支 `agent/scan-reliability`、标签 `v0.7.0rc4`，具体提交以该标签为准。发布后重新下载最终 ZIP 核对 SHA-256、包内版本与空数据自检，并验证 rc3 在试用通道能发现 rc4、正式通道不提示该试用版；最终复验结果保留在本机发布记录。

当前候选保留 RC 限制：实际在用 GUI 的退出更新重启、真实邮箱只读回归、至少三天长期试运行与 Windows 签名尚未完成，自动化和浏览器预览不能替代这些验收。

升级保留任务、数据位置和系统凭据。新增扫描覆盖表为缓存状态，旧版数据首次使用本版时保守补扫最近 30 天；过滤记录与原有待处理存储格式兼容。功能分支通过 PR 合并前，不将试用发布描述为已合并主线。
