# Windows GitHub 更新候选验收

日期：2026-09-20。范围：Windows x64 预发布，macOS 不作为本轮交付目标。

- 本机隔离目录完整回归：568 passed、1 skipped；跳过项依赖用户快照，本次未导入真实数据。
- Windows 更新专项 28 项通过，包含实际 PowerShell 文件夹替换与失败回退；启动进程使用替身，不关闭用户正在运行的程序。
- 真实访问本仓库公开发布，下载 rc1 ZIP、校验 SHA-256、解压并运行包内 CLI 空数据自检通过。
- 浏览器模拟桥接验证：新版提示、设置入口、下载完成、重启确认、失败提示及重试按钮；125% 字体显示检查。模拟操作不安装程序或连接邮箱。
- 发布工作流必须再次通过测试、秘密扫描、Windows 打包、空数据 smoke、资产数量与 SHA-256 检查后才创建 Release。
- 新增更新器只访问固定仓库公开发布；每日检查默认关闭。下载和安装需要主动操作，数据目录保持独立，旧程序目录保留备份。

尚未完成真实在用窗口的自动退出重启验收、长期试用、代码签名及系统安全软件兼容性覆盖。因此本次仅为未签名 prerelease，不宣称正式稳定版。

发布后应核对 Release 两个资产，并下载最终 ZIP 验证校验和与空数据启动。记录采用虚构测试数据，不读取授权码。
