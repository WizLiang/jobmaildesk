# Changelog

JobMailDesk 使用语义化版本号。每个可验证阶段必须先更新本文件，再提交并推送到 GitHub。

## [Unreleased]

## [0.7.0rc2] - 2026-09-20

- 新增 Windows GitHub 更新入口，固定读取 WizLiang/jobmaildesk 的发布；每日检查默认关闭，支持试用／正式通道，保留主界面不常驻显示版本号的偏好。
- 下载与重启均需用户操作；检查 SHA-256、包结构和解压路径，在同磁盘准备新目录，等待旧程序退出后替换，替换失败尝试回退并保留旧目录。
- 更新缓存位于用户选择的数据目录，不发送邮箱、求职记录或凭据；更新操作与隐私清除、数据迁移互斥。

## [0.7.0rc1] - 2026-09-20

- 按用户要求尝试 Windows GitHub 预发布：只发布 x64 ZIP 和 SHA-256，增加包内空数据自检；macOS 改为手动可选检查，不阻断 Windows 分发。

- 准备独立 GitHub 源码仓库：增加构建产物、本地配置、密钥和运行数据的忽略规则，以及 Windows SSH 接入和源码同步说明；首页改为当前定制版功能与构建入口。
- 首次上传采用单仓库 Deploy key；删除维护记录中的个人任务编号及日程映射，停用缺少 Android 源码的旧工作流，更新快速开始中的存储、扫描和升级说明。
- 首次导入分支推送时也执行三平台构建，便于在 PR 创建权限未配置时查看验证结果。
- 修复 macOS CI 在错误工作目录执行打包脚本导致图标和启动文件路径失效的问题；从源码根目录构建并显式指定产物目录。

- 待处理卡片和复核窗口显示具体归属原因及识别来源，区分字段缺失、信息冲突、多条候选和正常待确认。
- 新增单次 7／30／90 天历史补扫，重新检查范围内未确认及未识别邮件，保持日常设置和人工状态；扫描结果展示新增、更新、过滤与失败数量。
- 新增手动忽略邮件列表及按记录恢复。恢复沿用原 ID、递增修订号，并同步去重索引；手动恢复的记录不会被自动扫描或补扫覆盖、重新过滤。

- 公司／岗位识别补齐“请参加某公司的某岗位在线考试”、带括号的公司全称及线上笔试标题、简历通过我司“岗位”筛选等句式；拆分“深圳岗／南京岗位”等工作地点，并支持招聘单位标签。
- 清理合法公司全称末尾的招聘标签；避免把“系统”“线上”“的 AI”当成公司或岗位。冲突身份继续等待确认。
- 解析版本升级至 2026.09.20.1；下次扫描重查最近 30 天的待确认邮件，沿用原记录 ID，保留人工忽略及已确认状态。

- 设置页移除当前版本显示；保留内部版本信息用于诊断及构建记录。

- 扫描增加实时阶段与实际完成数量，覆盖自动、手动和托盘扫描；主界面及设置页均可查看，失败或退出时结束进度。读取与识别分别计数，进度不包含邮件内容或授权码。
- “同步台账”更名为“导入台账修改”，说明其读取本地 Markdown 手工修改并刷新卡片的用途，未配置台账时给出提示。
- 移除在线版本检查模块、设置页公告与下载入口、托盘检查菜单及安装程序中的上游链接；旧更新开关无效。

> 0.7.0rc1（2026-09-17）为 Windows 版候选构建，包含本节全部条目。

### Added

- 邮箱设置新增“保留有明确时间和线下地点的宣讲会”开关，默认关闭。切换后按新偏好复查最近 30 天，自动过滤与手动忽略分开保存；前者可按开关恢复，后者及已确认记录不复活。公开活动的“直通 Offer／现场面试”不再当作个人 Offer 或面试阶段。
- 设置页和扫描状态展示实际回看天数、读取与候选数量，设置页保留首次扫描读取数量及过滤／去重统计；旧记录没有记录过的范围明确显示为未知。
- 修复首次扫描错误套用旧版邮件去重迁移、导致超过 48 小时的邮件被提前跳过的问题。现在只对确实存在旧 Message-ID 处理记录的邮件应用兼容基线；新邮箱首次扫描完整识别近 30 天，解析器 `2026.09.19.2` 会补做窗口内此前被跳过的邮件。

- 默认过滤宣讲会、岗位推荐、邀投简历和校招宣传；取消“有线下场地就放行”的规则，保留真实投递回执、个人笔试／面试及进度通知。解析器版本升级为 `2026.09.19.1`，复查最近 30 天时自动忽略尚未确认的宣传记录，并撤下其未读提示；已确认、手动忽略及正文读取不完整的记录不自动撤下。
- 邮箱设置说明首次扫描最近 30 天、日常回看与定时扫描的区别；扫描包含已读和未读邮件，关闭窗口留在托盘时继续扫描。

- Windows 首次打开先选择统一的数据存放位置，再自动创建 `JobMailDeskData`；设置页支持更换目录并重启迁移。复制校验后才删除旧副本，自动更新内部 Markdown／研究队列路径，外部 Obsidian 文件保持不变。数据位置由当前用户注册表记住，升级便携包无需重装或逐项改路径；失败请求可重试。
- 设置页明确 Obsidian 为可选功能，以及当前尚无 Notion／语雀自动同步接口。

- Windows 设置新增“清除个人信息”：输入“清除”确认后退出旧进程，删除新旧邮箱凭据、私人链接密钥及应用管理的本地记录、配置、简报、学习规则、日志和 WebView2 缓存，再重新打开首次设置；失败时保留清除请求并阻止扫描，下次启动重试。外部文档、手动备份和已导入系统日历的事件需另行清理。
- 交付目录移除旧用户快照、数据库、历史简报、学习密钥、安装日志和旧构建；测试样例中的姓名替换为虚构姓名。安装包不再附带个人快照。

- 复核窗口与主窗口归属弹窗新增「更新已有待办」模式：改期 / 改场地类邮件可选择目标申请链上已有的待办，把邮件里的时间、阶段、行动内容和原邮件定位原位写入该待办（`change_type: update`），不再为同一次面试建第二张卡；后端自动按阶段（邮件指明轮次时只认同一轮次）推荐要更新的待办，并带乐观锁防止覆盖刚被手改的待办；邮件不带时间时保留待办原时间，空备注不会覆盖已写的备注。
- 扫描发现新的待确认邮件时弹出系统通知（定时扫描与托盘 / 界面手动扫描均会，可在设置中关闭）；Windows 上点击托盘气泡通知打开主窗口并切到「待处理」，提醒类气泡点击只显示窗口，PowerShell Toast 回退不带点击动作。
- Windows：关闭窗口改为隐藏到托盘，程序在后台继续扫描与提醒，只有托盘菜单「退出」才真正退出（关闭钩子安装失败或设置环境变量 `JOBMAILDESK_CLOSE_EXITS=1` 时恢复为关闭即退出，设置页会如实显示）；新增「在任务栏显示窗口按钮」设置（仅 Windows 显示，默认开启，带最小化 / 最大化）。

- Windows 提醒改为可见通知：优先经托盘图标气泡（pystray），无托盘或失败时回退到 Windows PowerShell 的 WinRT Toast；未送达时返回失败，不再把阈值标记为已发送。
- Windows 托盘图标显示去重后的唯一未读数角标与悬浮提示，对应 macOS Dock 角标；`set_dock_badge` 契约不变。
- Windows 系统日历改为导出 RFC 5545 iCalendar 文件 `%LOCALAPPDATA%\JobMailDesk\<日历名>.ics`（UID 为 `jobmaildesk:<task.id>`，UTC 时间，原子写入）；设置页显示文件位置并可打开所在文件夹。
- 新增 `jobmaildesk smoke`：包内自检（运行时导入、tzdata、词典计数、身份 golden 向量、事实层往返、可选凭据库往返、事实数量核对），供安装脚本对构建产物与已安装程序执行。
- 新增 `jobmaildesk import-snapshot`：把另一台机器的数据目录导入本机，跳过 `.transactions/`、锁文件、日志与派生缓存，改写 `config.toml` 中的旧根目录路径，并核对数量。
- 启动前检测 Edge WebView2 运行时；缺失时弹出提示并打开官方下载页，而不是静默退化到 IE 引擎。
- 打包：Windows 改为 onedir，同时产出 `JobMailDesk.exe`（窗口）与 `JobMailDesk-cli.exe`（控制台）；补齐图标、版本资源、`tzdata`/`_cffi_backend`/WebView2 程序集收集、`-X utf8` 运行时选项；新增 Inno Setup 脚本与一键安装脚本。

- 将“待确认”和本地 `unresolved` 缓冲区统一为组件“待处理”入口；待归属邮件会显示候选申请链，可直接归属或二次确认忽略。
- 新增“同步台账”按钮与 `jobmaildesk sync-ledger` 命令，只读取本地岗位台账并刷新卡片，不连接邮箱。
- 增加独立求职进展台账模板文档，明确稳定申请 ID、可编辑字段和“已归档”规则。
- 邮件归属弹窗支持在创建或更新申请链的同时建立关联待办，并自动复用邮件解析出的时间。
- “速览”首页按 72 小时重点、本日、本周和最新待归属邮件分区展示，并提供待办回收站、恢复与永久删除墓碑。
- 设置页增加 90%–125% 字体缩放，卡片长文本继续使用安全省略，避免窄窗口错位。
- 公司/岗位识别增加字段级证据、置信度、脱敏 golden vectors 和可停用的本机纠错学习；支持从最近 30 天已确认申请重建学习库。
- 六个主视图增加持久未读红点与高亮，进展按未读公司逐卡确认；macOS Dock 显示去重后的总未读事件数。
- 待处理邮件支持同时打开多个独立原生复核窗口，并列核对按需读取的原邮件与申请、进度及可选待办。

### Changed

- 待办卡片的完成、恢复、改期、改阶段现在会同步回申请链：关联的邮件复核节点状态与阶段跟随待办，申请链当前节点状态随待办完成 / 重开，待办进入更深阶段时当前阶段随之推进；已结束 / 已归档的申请链完全不动。进展卡保存只在明确改了节点状态、且当前待办就处于该阶段时才联动待办，不再把刚完成的待办改回未完成，把链推进到新阶段也不会重开上一阶段的待办。
- 申请时间线把描述同一阶段的邮件复核节点并入待办行，已回收 / 已忽略待办的节点不再以「未完成」重新出现；应用详情与进展卡使用同一套过滤。
- 状态变更、推迟后立即刷新日历导出与提醒，不再等下一次 10 分钟任务；给过期的邮件待办改时间会让它回到日程。
- 事实层读取改为 libyaml 解析并按文件 `(mtime, size)` 缓存（本进程写入时同步失效），文件事务对备份文件只并发 fsync 一遍（原先串行刷三遍）、标记文件单独刷：标记完成从约 9 秒降到 1 秒以内，确认归属从约 11 秒降到 1 秒以内（94 待办 / 59 申请链 / 126 待处理的真实数据；实测数字见 docs/08）。备份在 `prepared` 标记落盘前已持久，崩溃恢复不会用半截备份覆盖事实文件。
- 窗口隐藏时前端停止周期刷新，重新显示或获得焦点时立刻拉取一次（包括第二个实例唤起、托盘「设置」/「检查更新」、复核窗口保存后）。
- 目录 fsync 统一走 `fs_utils.fsync_directory`（Windows 不允许打开目录句柄，跳过即可）；文件事务改用可写句柄 fsync，回滚/恢复对瞬时文件锁重试，回滚失败时保留备份供下次启动恢复。
- Windows 凭据库后端与 macOS 分支对称地显式绑定为 `WinVaultKeyring`。
- Windows 文件锁争用在有界重试后抛出明确的中文 `DataLockBusyError`，不再泄漏 `Errno 36`。
- pywebview 在 Windows 固定使用 `edgechromium` 后端，WebView2 用户数据目录收进 `%LOCALAPPDATA%\JobMailDesk\webview2`。
- IMAP `SINCE` 日期改用固定英文月份表，不依赖进程 locale；CLI 在 Windows 强制 UTF-8 标准输出。
- `package-core.ps1` 打包整个 onedir 目录，并允许预发布版本回退使用基础版本号的验收记录。

- `progress.source_path` 正式作为可编辑控制面：修改企业、岗位、当前进展和下一步动作后，进展卡片会使用台账内容；`progress.output_path` 继续作为只读快照。
- 台账中的“已归档”加入终止状态识别，保留历史节点但停止将申请视为进行中。
- 手工待办弹窗统一为申请工作流样式，可绑定稳定申请链并触发日历与通知刷新。
- 周历与月历增加任务日期底色、内边框、数量徽标以及 today/selected/urgent 组合状态。
- 展开申请卡操作固定为 3+2 等宽布局；申请链合并支持搜索全部公司、按公司分组和跨公司明确确认。
- 公司与岗位提取由“首个正则命中”改为多候选评分；标题显式字段、正文句式、发件人、词典和模板分别取证，营销 CTA、招聘平台词和年份/批次不再混入身份字段。
- 自动新建申请要求可信公司证据及明确岗位、项目或职位编号；公司加年份、岗位子串等弱证据只参与候选排序。
- 所有新招聘邮件统一先进入待处理，自动识别仅负责预填和推荐；一次人工确认再原子创建/更新申请、进展节点及可选待办。

### Added

- 新增笔试/面试/宣讲会日程训练集 `tests/golden/schedule_vectors.json`：49 条脱敏真实邮件，含人工标注的开始、结束、硬截止、时长与公司，长期保存并纳入回归，便于反复训练识别算法。

### Fixed

- `activity_store` 在 Windows Python 3.12 上因缺少 `os.fchmod` 抛出 `AttributeError` 的问题。
- 两处依赖固定日历日期的测试夹具（IMAP 假服务器的 INTERNALDATE、翱捷重放测试）在 2026-09-14 后失效的问题，改为相对当前时间。
- 一处仅在东八区本机时区通过的日历测试改为按本机时区推算期望值。

- 修复新建或编辑待办时同步推送整个待办库到系统日历，导致保存要等几十秒的问题；每条事件都要单独启动 osascript，首次还会阻塞在授权弹窗上。日历写入改到后台线程并合并连续请求，保存只负责写入事实。
- 修复待处理记录读取时按裸 `---` 切分 frontmatter，邮件页脚分隔线（如 `----------------`）被收进字段后会截断 YAML，导致整个待处理库无法读取的问题；现在只在整行恰为 `---` 处切分。
- 修复待办编辑器时间显示比卡片少 8 小时的问题：预填直接截取 UTC 字符串，未换算本地时间。
- 修复部分 ATS 邮件只发一个占位 `text/plain` 部件、真正的面试时间表格仅存在于 HTML 半边，导致时间在解析前就被丢弃、待办显示“时间待确认”的问题；现在按去除链接后的实际信息量选择部件。
- 修复时间识别缺陷：`10:00 -- 11:40` 双短横线区间、带秒时间戳后紧跟“失效”的上下文、“收到本链接后72小时内”“请您于24小时内”“将于24小时后失效”“3个工作日内”等相对截止说法，以及“作答有效期”被误当作开始时间。工作日按自然日计。
- 修复公司名被识别为 HR 个人姓名（栾宁/温煦/Amy）、ATS 平台名（iTalent招聘助手/boss直聘）、系统账号（recruitment/SGMICRO_Exam）或宣讲场地（东南大学（无锡校区））的问题；改为综合发件人、发件域名、标题与正文扫描已审核公司词典命中，并识别姓氏开头的人名。
- 线下校园宣讲会邀请不再被整类丢弃；带具体场地的宣讲会进入待处理，线上直播空宣继续排除。
- 修复退出时正在进行的邮件扫描被中断后，`imaplib` 的 LOGOUT 在已关闭的 socket 上抛出 `IMAP4.abort`，把取消伪装成扫描失败弹给界面、且跳过 socket 释放导致描述符泄漏的问题；中断现在稳定还原为取消，真实的邮箱故障仍按原样上报。
- 修复点击退出后窗口转圈、进程长时间不结束只能强制退出的问题。窗口回调不再同步执行调度器 join、复核窗口销毁和 IMAP socket 关闭；清理移到工作线程，退出由看门狗在事实写入静默后有界结束，被取消的“退出”不再提前拆掉运行时。
- 修复“海光信息招聘”未归一为“海光信息”、`2027校招-` 混入岗位，以及复核窗口用公司和岗位联合过滤而隐藏同公司手工申请链的问题；目标搜索现在先按公司展示并允许人工归入岗位名称不同的已有链。
- 完成最近 30 天 48 封真实邮件的逐封只读审计，修复 MIME 解码、超大附件、逐 UID 重放、字段证据优先级、批次/轮次/时间解析、重复进展以及验证码/问卷/空宣误报；最终影子扫描达到 48/48、0 重试、0 解析失败。
- 修复明确的“感谢您投递某公司某岗位”标题因正文营销页脚含“云宣讲/邀你赴约/抢先拿 offer”而被整封提前判为非招聘邮件的问题；RC3 同时记录不含正文的安全解析诊断，避免再次盲目重放。
- 修复翱捷解析规则已更新但 parser version 未同步提高，导致 UID 1709047101/1709047102 继续沿用旧 `noncandidate` 结果而在扫描中全部跳过的问题；RC2 会只重放未形成事实的近期邮件。
- 修复计划任务刚刚越过时间、扫描器尚未写入 `expired` 时 Markdown 导出可能因未知分桶中断的问题。
- 修复新公司邮件仍显示全部历史申请、无法正确编辑预填身份的问题。
- 修复旧公司旧岗位邮件只能归属、不能同时更新节点并创建待办的问题；未创建待办的邮件现在仅保留为时间线信息节点。
- 修复待确认邮件二次哈希原始来源标识导致重放去重关联变弱的问题。
- 修复现代 setuptools/py2app 与 uv standalone Python 组合下的 macOS 打包失败，并阻止 Android 工具链被误收入应用包。
- 恢复 macOS 应用图标，并继续使用既有 `fixed-vN` 安装包命名格式。
- 统一 macOS Bundle 名称、显示名称与可执行文件为 `JobMailDesk`，不再依赖构建后重命名应用目录。
- 修复未配置邮箱授权码时整个后台调度器未启动、导致本地手工待办无法发送 macOS 通知的问题。
- 修复合并目标元数据被进展快照覆盖而丢失 `same_company`、同公司不同岗位无法选择的问题。
- 申请链合并现在迁移双方 legacy ID、来源哈希、人工时间线、任务和待归属引用；写入失败会回滚事实文件。
- 修复 IMAP 无超时且后台扫描线程无法取消，导致 macOS 点击退出后进程长期卡住的问题；手工和定时扫描现在共享门闩，退出会中断活动只读连接。
- 修复“您已应聘思特威电子科技，快来Get最新招聘信息查询方式”把“快来Get最新”截成公司或岗位的问题。
- 修复思特威笔试邀请把岗位识别为“】在线”、把 8 月 14–17 日作答窗口压缩为 90 分钟的问题；作答时长现在独立保存，截止仍为 8 月 17 日 23:55。
- 修复仅填写结束时间的手工待办仍进入待处理、补时间后状态不更新、无法进入日历和提醒的问题。
- 修复待办弹窗主动缩放/移动原生窗口及刷新重建列表导致的页面跳动和滚动丢失。
- 修复浏览器原生表单校验静默拦截空“下一步行动”，造成点击保存看似无响应的问题。
- 修复加密行动链接引入后 macOS py2app 包遗漏 `_cffi_backend`、仅在打包应用启动时崩溃的问题。
- 修复明确标题 `【江波龙】` 被发件人显示名以近似分数冲突覆盖，导致公司和“已成功申请”岗位同时为空的问题。
- IMAP 去重改用“账号指纹 + 文件夹 + UIDVALIDITY + UID”；不再仅信任可能被招聘平台重复使用的 `Message-ID`，并安全回收最近 48 小时被误跳过的新邮件。

## [0.6.0] - 2026-08-04

### Added

- 设置页新增 QQ、网易 163、网易 126、Yeah、Gmail、Outlook 与自定义 IMAP 服务商选项；选择预设后仍可编辑主机、端口与 SSL/TLS。
- 邮箱连接测试会直接使用尚未保存的设置表单，便于用户在保存前验证自定义 IMAP 参数。

### Changed

- 邮箱字段统一改为“邮箱账号”和“客户端授权码”，不再将产品界面限定为 QQ 邮箱。
- 旧版仅包含主机与端口的配置会按 IMAP 主机自动推断服务商；无法识别时安全归入自定义配置。
- IMAP 客户端会根据设置选择 SSL/TLS 或普通连接，两种方式都继续保持只读文件夹与 `BODY.PEEK` 获取。

### Security

- 将 `cryptography` 依赖升级并约束到 `>=50,<51`，修复 49.x 安全版本范围问题，并锁定 Windows/Python 3.12 使用官方 wheel。

## [0.5.0] - 2026-08-04

### Added

- 内置 520 家企业、129 个招聘项目和 2825 个岗位名称，Core 无需额外模型即可参与企业、项目和岗位标准化。
- 设置页增加“企业与岗位词典”，支持选择个人 XLSX 秋招表、指定工作表并在本机编译启用；源表不会复制进程序数据或公开仓库。
- 正式启用 Application Registry 批次归属和本地 `unresolved` 缓冲区；新增 `unresolved-list`、`unresolved-resolve` 与 `unresolved-ignore` 命令完成人工闭环。
- 任务增加可选 canonical `application_key`，同时保留旧 `application_id`，支持旧任务和 Obsidian 标记渐进迁移。
- 增加“任务状态 → 求职进展/人工台账”的受控同步：组件或本地 Agent 完成任务时，程序按稳定申请 ID，或唯一的“公司 + 岗位/J 编号”匹配台账行，只更新当前进展并保留复选框、下一步动作和手写内容。
- `求职当前进展.md` 的申请链与流程节点现在分别携带稳定申请 ID、任务 ID和可见复选框，便于核对某次组件操作实际更新了哪个任务。
- 任务首次完成时记录独立的 `completed_at`；进展时间线显示实际完成时间，不再把测评有效期结束时间误当成完成时间。
- 旧版本已经完成但没有 `completed_at` 的任务会从最后状态更新时间回填，并标记为“历史推定”；无可靠时间时不使用截止时间替代。任务 Markdown 模板同时显示开始、结束/截止和完成时间。
- `求职当前进展.md` 改为每个岗位一张默认折叠的 Obsidian 卡片，展开后以结构化表格显示岗位、阶段、轮次、活动窗口、截止、完成、下一步和流程记录，替代难以浏览的长行文本。

### Changed

- 邮箱扫描改为“整批解析 → 身份解析 → 任务或待归属记录”，不再逐封调用旧的模糊申请 ID 推断；京东通用回执等 `creates_application: false` 模板不能独立创建申请链。
- 词典优先级固定为“内置基础词典 → 导入词典 → 旧版根目录覆盖 → manual 人工覆盖”，人工规则始终优先；大词典查找改为预索引并统一 NFKC 标准化。
- XLSX 编译增加 50 MB 文件、200 MB 解压体积和 100000 行安全限制，并通过 staging 校验后再原子替换导入层。
- 增加 Release Freeze 本地验收闸门：同一轮反馈只使用本地 RC、PR 和 Actions 构建产物迭代，全部本地验收结束并标记 `release-ready` 后才一次性创建新 Release。
- 进展台账模板补充受控同步说明；同公司多岗位只按岗位或职位编号精确关联，不再把一条人工进展套用到该公司的所有申请链。

### Fixed

- 修复预览版本号（如 `0.5.0.dev1`、`rc1`）被更新检查器拒绝的问题，并按开发版、测试版、候选版、正式版正确比较。
- 修复 Obsidian 临时占用人工台账时 `WinError 5` 中断整批邮箱扫描的问题；本地写入会短暂重试，台账仍被占用时跳过本轮并在下轮补同步。
- 修复点击设置时原生求职卡片先被放大、随后才显示设置弹窗的视觉跳变；设置页改为在当前纸片尺寸内滚动显示。
- 修复通用网申回执和同公司多项目邮件被拆成独立申请链的问题；存在多个旧 ID 或硬身份冲突时现在 fail closed，进入待归属而不是猜测。
- 修复旧任务只保存 `application_id` 时无法加入新 Registry 分组的问题；唯一旧 ID 会自动回填 canonical key，冲突映射保持不变等待人工处理。
- 修复邮件申请链与人工台账因 `TET 综合方向/TET 管理培训生（综合方向）`、`网易游戏/网易雷火` 等受控别名未统一而重复生成进展卡的问题。
- 修复“确认投递的是雷火事业群校招职位”等说明句被识别为岗位，以及网易互娱“您投递的 AI策略运营 职位”未提取岗位的问题。
- 台账同步改为整批邮件归并完成后按申请链当前节点写入一次，避免旧 AI 面试覆盖更新的群面状态；解析规则升级时自动回放 30 天历史窗口。

- 修复招聘邮件中的客服电话与服务时间被误当成日期、触发 `month must be in 1..12` 并中断整批扫描的问题；非法日期片段现在会被跳过，单封解析异常也会被隔离而不阻塞后续邮件。
- 京东 TET 综合面通知会按“群面 / TET 综合方向”归类，并读取邮件中的真实面试时间，不再把电话号码附近的时间作为活动时间。

## [0.4.2] - 2026-08-01

### Fixed

- 修复 Obsidian 未勾选任务在扫描回读时把已确认、无具体时间的申请进展错误降级为“待确认”的问题。
- 修复已过期任务仍出现在桌面待办与 Obsidian 待办总览的问题；历史节点继续保留在企业进展中。
- 修复无明确时间、仍在简历池或等待补充信息的记录同时进入“待确认”和“待办”的问题；现在只保留在待确认与企业进展。
- 升级桌面快照结构版本，安装新版本后立即重建视图分类，不复用旧版待办缓存。
- 增加“收到邮件后 N 小时/天内”相对截止解析；超过 7 天仍无时间的旧测评、笔试和面试通知自动退出注意区并保留为进展历史。
- 清除 Core 扫描器残留的自动 Research 队列逻辑，旧配置中的 `research.enabled=true` 不再生效或阻断邮件同步。
- 修复 `在线`、`AI`、`27届校招` 等通用招聘词被误识别为企业名称的问题；新增本地精确别名与通用词标准化词库。
- 修复流程说明中的 `offer/笔试` 被误识别为当前阶段、官网操作句被误识别为岗位、软性拒信仍停留在待确认的问题。
- 支持“邀请于 A，于 B 失效”的测评有效窗口；网易雷火与网易互娱统一归入网易游戏总览，但始终保留独立事业群申请链。

### Changed

- 首次成功扫描自动回看最近 30 天，之后恢复用户配置的短窗口扫描；显式 `--days` 参数仍优先。
- 待确认页增加“全部、近 7 天、简历筛选、测评/笔试、面试”胶囊筛选栏。
- 建立完整维护与发布制度，明确阶段提交、主线合并和产品 Release 的区别，并加入分支、语义化版本、PR 闸门、兼容迁移、紧急修复和回滚规则。
- 新增 Pull Request 模板，将测试、秘密扫描、隐私兼容、Changelog、发布判断和回滚说明变成每次变更的固定检查项。
- README 首屏新增“5 分钟开始使用”，明确普通用户从下载、QQ IMAP 授权、首次配置、可选 Obsidian 连接到日常操作的完整路径。
- 重构README为面向终端用户的产品首页：优先展示三平台下载、首次配置、核心能力、隐私边界与更新方式，并将CLI和构建说明收纳到开发者折叠区。
- 增加系统下载表、工作流图、功能矩阵、Core/Research边界和签名路线，减少首次使用者在安装依赖与数据去向上的判断成本。
- README首屏升级为居中的产品标题、动态版本、跨平台、运行时、UI、构建和许可证徽章。
- README按产品预览、设计原则、核心能力、操作手册、设置、数据隐私、下载安装和开发文档重组，并加入三组可复现的匿名高清界面预览。
- 将独立许可证升级为可被GitHub识别的标准`LICENSE.md`，并新增中英文`docs/LICENSING.md`说明许可场景、再分发清单和第三方权利边界；发布包同步携带两份文件。

## [0.4.1] - 2026-08-01

### Fixed

- 统一版本通知区两个操作按钮的字号、高度、边框和禁用态，修复“打开下载页”误用系统原生大字号样式的问题。
- 将面向用户的“打开 Release 下载”改为更简洁的“打开下载页”，避免中英文混排造成视觉跳脱。

## [0.4.0] - 2026-08-01

### Added

- 设置页与Windows托盘增加版本通知入口，支持预览版/稳定版通道、每天一次后台检查和Release Notes。
- 只在本项目Release同时存在当前平台ZIP与同名`.sha256`时提示可用版本，并提供对应下载页。
- 新增版本检查结果缓存、手动更新入口和完整网络/隐私说明；程序不自动下载、解包或安装。

### Changed

- 发布工作流和Windows打包脚本从项目元数据读取版本，避免多处手工修改资产名。
- GitHub Actions升级到Node.js 24运行时的官方主版本，消除Node.js 20弃用警告；Release下载同时启用新版本默认的artifact digest强校验。
- Core仍不依赖GitHub运行；更新请求可关闭，网络失败不会阻塞邮件、任务、日历或Obsidian。

## [0.3.0] - 2026-08-01

### Added

- 首次运行配置向导和常驻设置页面，支持 QQ IMAP、扫描周期、Obsidian 输出与求职进展路径。
- `task-list` / `task-update` 本地 Agent 接口：按稳定任务 ID 写回完成、恢复、改期、忽略和详情字段。
- 周历、月历、企业进展、待确认和待办视图；完成项保留并可恢复。
- 侧边胶囊、跨屏边缘吸附、托盘入口、单实例 `show` 唤回和缓存首屏。
- Windows x64 self-contained 构建；GitHub Actions 中的 macOS Apple Silicon / Intel 构建链。
- 求职进展模板、独立进展文档及 Obsidian 稳定 ID 双向勾选同步。
- 匿名解析回归：百度考试窗口、海信跨日窗口、科大讯飞测评有效期、慧策旺店通 `24:00` 简历截止。

### Changed

- 版本标签构建在三个平台全部成功后自动创建 GitHub prerelease，并上传 Windows x64、macOS Apple Silicon、macOS Intel 的 ZIP 与 SHA-256；缺少对应版本 Release Notes 时拒绝发布。
- GitHub Actions在指向`main`的Pull Request中自动运行Windows与macOS双架构构建，确保首次引入工作流时也能完成远端验收。
- 将py2app配置隔离到`packaging/macos/`，避免根目录PEP 621依赖被映射为py2app已禁止的`install_requires`。
- 将PyInstaller限定为Windows开发依赖，并从macOS包显式排除，避免py2app误收集Windows构建工具及其可选Qt hooks。
- Core 默认完全本地、无需模型/API/境外网络；Research 改为显式启用的可选边界。
- 桌面卡片和 Agent 更新统一调用同一个同步服务，实时刷新任务 Markdown、日历、进展和 Obsidian。
- 解析器升级采用版本化回放与消息哈希修正，忽略项和已完成项不会被重复扫描复活。
- 启用 Per-Monitor DPI v2；窗口默认尺寸与编辑尺寸调整为更适合详情维护。
- 从 Core 安装包移除已经取消的独立待办纸、笔记纸及其前端资源；旧版本本地 Markdown 数据不会被删除。

### Fixed

- 修复 pywebview 暴露原生窗口对象导致的启动白屏、卡顿和递归辅助功能错误。
- 修复重复启动生成多套托盘、调度器和数据库连接的问题。
- 修复退出时 `Scheduler is not running` 未处理异常。
- 修复 QQ 招聘邮件中的 `24:00`、有效期、条件拒绝词和模板噪声误判。
- 修复完成、忽略或改期后重新扫描产生重复卡片和重复研究请求的问题。

### Security

- IMAP 保持 `readonly=True` 和 `BODY.PEEK`，不修改未读状态。
- 凭据只进入 Windows Credential Manager 或 macOS Keychain。
- 发布秘密扫描覆盖邮箱、个人路径、授权码、私人 URL 和长数字标识。
